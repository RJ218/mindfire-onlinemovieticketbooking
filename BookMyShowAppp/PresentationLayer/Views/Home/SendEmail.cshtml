<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>sucessfull_payment</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css" integrity="sha384-9aIt2nRpC12Uk9gS9baDl411NQApFmC26EwAOH8WgZl5MYYxFfc+NcPb1dKGj7Sk" crossorigin="anonymous">
    <link href="~/Content/css/BookSeats.css" rel="stylesheet" />

</head>
<body style="background-color:white !important;position:relative;top:-100px;">
    <div class="jumbotron">
        <div class="card">
            <div class="card-body">
                <div class="card-title"><h1>Booking Successfull</h1></div>
                <hr />
                <div class="card-body">

                    <h4 id="moviename"></h4>
                    <h4 id="theaturename"></h4>
                    <h4 id="showtiming"></h4>
                    <div class="row">

                    </div>
                </div>


            </div>
        </div>
    </div>

    <script>
        var ThData = 1;

                    //console.log(bookedseats)
                    var booked_string=localStorage.getItem('bookedseats');
        var bookedseats = [];
        var temp = '';
        for (let i = 0; i < booked_string.length; i++) {
            if (booked_string[i] == ',') {
                bookedseats.push(temp);
                temp = '';
            }
            else
                {
                    temp += booked_string[i];
            }

            if (i == booked_string.length - 1) {
                bookedseats.push(temp);
        console.log(bookedseats);

            }

        }


                    for(var i=0;i<bookedseats.length;i++)
                    {
                        if (i % 6 == 0) {
                            document.getElementsByClassName("row")[0].innerHTML += "<br/>" + "<div class='seat' id="+bookedseats[i] + ">" + bookedseats[i] + "</div>";
                        }
                        else { document.getElementsByClassName("row")[0].innerHTML += "<div class='seat'  id=" + bookedseats[i] + "> " + bookedseats[i] + "</div>"; }

                    }



        document.getElementById("moviename").innerHTML = localStorage.getItem('moviename');
                document.getElementById("theaturename").innerHTML = localStorage.getItem('theatrenm');
                document.getElementById("showtiming").innerHTML = localStorage.getItem('time');

    </script>
</body>
</html>

